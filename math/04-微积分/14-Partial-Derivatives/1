偏导数与梯度：多个变量怎么优化

现实问题通常不只有一个变量。

例如，利润 P 取决于价格 p 和广告投入 a：
P = P(p, a)

这时，我们就需要分别研究：
∂P/∂p  
表示在广告投入 a 不变时，价格 p 的变化对利润的影响有多大。

∂P/∂a  
表示在价格 p 不变时，广告投入 a 的变化对利润的影响有多大。

这两个量分别叫做 P 对 p 的偏导数、P 对 a 的偏导数。

怎么理解“梯度”？你可以把对每个变量的偏导数上下摆成一个向量（也可以看作是特殊情况下的“求导矩阵”，一般多输出才叫雅可比矩阵，这里是一列矢量）：
∇P = [
  ∂P/∂p  
  ∂P/∂a  
]
它其实就是“各个方向分别怎么变最快”综合成的一个“箭头”。

怎么看梯度方向？梯度指向函数值增长最快的方向。比如你站在山坡上，这个“梯度箭头”指向陡坡往上的路。你往这个方向走，函数增加得最快；反过来，往负梯度的方向走，函数减小最快，比如机器学习里参数要不断“降损失”就是走负梯度。

那如果你用梯度下降法不断更新，最终会不会循环到原点？——一般来说，不会直接“循环到原点”，而是不断向着某个“最低点”（极小值）靠近。只有极特殊的函数/初始点或者不合适的更新策略、学习率下可能出现周期、震荡，或者根本没有“最低点”才有可能“绕圈”，大多数常规问题最终都会收敛到某个“极值点”（比如极小值、鞍点、或者平坦区）而不是一直转圈。

小结：“梯度”就是把所有偏导数像“上下左右求导”那样组合，是你在“多维空间”里最快变大的方向。走负梯度，一般会走向最低点，而不是循环到原点。

机器学习里常用的“梯度下降法”原理就是：
新参数 = 旧参数 − 学习率 × 梯度

总结：
- 多元函数的偏导数用了分开研究各变量的作用；
- 梯度向量把所有偏导数整合，给出最大变化率方向，是最优调整的指北针。
其实偏导数的难点主要在于：你要“只让一个变量变，其它都保持不动”来求导。实际做题时，可能公式会很复杂、多层嵌套，需要你对每个变量都敏感地区分对待。但原理还是类似单变量求导，只是“以其它变量为常数”这一点要习惯。

很多同学一开始会不习惯“看见那么多个变量”，比如：
f(x, y) = x²y + y³
对 x 求偏导，就是只把 x 当成变量、y 当常数——所以 ∂f/∂x = 2xy。而对 y 求偏导时，把 x 当常数，∂f/∂y = x² + 3y²。

等你熟悉后，会发现多元函数其实很好计算，只要记住这条“其它当常数”。

而梯度和梯度下降虽然名字酷炫，本质上其实就是“沿着最快降/升方向调整参数”——如果你已经理解了偏导和梯度公式，其实就自然水到渠成了。梯度下降法更难的是怎么选好步长（学习率），实际优化时换各种花样让收敛快、稳定，但理论思路并不神秘。

所以——
* 偏导是最基础的多元导数技巧，理清后你会发现梯度其实就是“打包整合”，
* 梯度下降法则是应用，把梯度当成指南针、慢慢走到最低点。

可以先多做点偏导的例题，习惯“假定其它变量为常数”，基础一通，后面就顺了。梯度其实并不抽象，就是把所有偏导“合成”变一个箭头。“下降法”是用到它来优化目标而已。

小结：偏导是概念基础，难点在习惯思路；梯度和下降法是它的应用，只要基础扎实，后续自然顺畅。